# Hide and seek

- **`hide-seek-web/`** — Vite + React + **styled-components** + React Router (Home, Classes, About Us). **Start here** for the PNG-based implementation.
- **`move-web/`** — earlier Next.js prototype (optional).

```bash
cd hide-seek-web
npm install
npm run dev
```
